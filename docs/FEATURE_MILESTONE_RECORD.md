# Completed Feature and Milestone Record

This file records only features and milestones that have met their definition
of done and completed required review. Follow
`docs/GENERAL_CODE_REVIEW_INSTRUCTIONS.md` when adding an entry.

Each entry must use this structure and contain exactly the two required prose
paragraphs beneath its heading. Replace the placeholders; do not retain the
labels as separate bullet points.

## `<Feature or milestone name>` — `<completion date>` — `<final commit>`

**Technical:** `<One paragraph describing the implemented behavior,
architecture, interfaces, data/schema changes, safety and compatibility
properties, and final validation in software-development language.>`

**Plain language:** `<One paragraph explaining the same completed
functionality, its value, and its limitations in terms a high school student
can understand.>`

---

Entries for milestones completed before this record existed (GR-0, GR-1A,
GR-1B, and earlier ML milestones) may be backfilled from
`docs/GENERAL_READINESS_STATUS.md` and `docs/ML_IMPLEMENTATION_STATUS.md`
when the owner prioritizes it; absence of an entry below does not mean the
milestone was not completed and reviewed.

## GR-1C — validation orchestration behind call-time dependency injection — 2026-08-02 — merge `2882889` plus follow-up review `c1de927`

**Technical:** GR-1C moved the 315-line body of
`validate_proposal_for_execution()` from the `assistant/execution_service.py`
facade into `assistant/execution_kernel/validate.py::run_proposal_validation()`
behind a frozen `ProposalValidationDeps` contract that the facade constructs
from its own module namespace on every call, so every historically
monkeypatchable facade name — the risk gate `validate_trade_intent`, policy
fingerprint, stored-intent parsing, pending-buy exposure, earnings resolution,
the deferred broker import, `datetime`, `timezone`, `Decimal`, `TradeIntent`,
`to_decimal`, the `ProposalValidationOutcome` constructor, and the
data-integrity/infrastructure failure-classification constants — remains a
live call-time seam after the move. The kernel function performs zero
module-global runtime reads, pinned by a symtable-based structural guard;
each injected seam has a behavioral characterization test proven red on the
pre-correction tree and verified to catch reverse-mutations back to
kernel-local resolution. Validation stays read-only (it reads durable state
and queries the broker; it never claims, transitions, reserves, or submits),
the check order is unchanged, exception identities and the full pre-GR-1C
facade import surface are identity-pinned, and the one deliberate residual —
the `resolved_failure_class` property's kernel-resolved fallback constants —
is documented and pinned by its own test. Completed across implementation
`b4d9b1f`, independent review `465df8d` (merged as PR #109/#110), follow-up
review `c1de927`, and an independent confirmation round that re-verified all
corrections red/green with a six-of-six mutation-detection sweep.

**Plain language:** Before the trading assistant acts on a trade the owner
approved, it runs one final safety checklist: is the approval still fresh, do
the rules still allow this trade, is the emergency stop off, is the price
quote current, and more than a dozen similar questions. That checklist used
to live buried inside one enormous file; this milestone moved it into its own
small, clearly-named module without changing a single question or the order
they are asked in. The subtle part is that the checklist depends on tools —
a clock, a price feed, the rulebook — and our tests work by swapping those
tools for fakes (a frozen clock, a pretend broker) to prove the checklist
reacts correctly. So the move was done by handing the checklist an explicit
toolbox, rebuilt fresh on every call: swap any tool in the outer layer and
the checklist genuinely uses your swap, exactly as it did before the move.
Automated tests now sabotage each tool on purpose and confirm the sabotage is
always noticed. The limitation to remember: this is internal plumbing that
makes the safety code easier to audit and harder to break by accident — it
adds no new trading abilities, and it is not evidence that any trading
strategy makes money.

## UI feature controls — 2026-08-03 — merge `4c8e959` plus independent review `a6d5254`

**Technical:** The Streamlit application now exposes seven tabs, adding a Settings & Features surface that separates session-only UI preferences, protected authoritative policy edits, and read-only provider/safety status, plus a dedicated research-only Ticker Suggestions surface. Optional-AI master and per-feature flags gate all four LLM call paths without changing deterministic content; `allow_new_positions` and `enable_strategy_proposals` updates validate a new frozen `TradingPolicy`, require the exact typed confirmation, atomically persist a bumped version and changed fingerprint, force a full-app rerun, and leave prior proposals fail-closed under the existing fingerprint check. The editor is bound to the selected policy path and content fingerprint, disabled suggestion sources make no provider call, and the dedicated tab avoids an unused paid curation call. No database schema or execution-kernel contract changed; paper mode, exact approval, kill switches, exposure checks, and live-trading friction remain intact. Independent review reproduced two P2 control-state defects red, corrected them, and validated 236 focused tests plus 2,427 passed, 1 skipped, and 26 warnings in the full Python 3.12.13 suite, with clean compilation and diff checks.

**Plain language:** The app now gives you one clear place to control optional features and see whether important safety systems and data providers are available. You can turn permission to open new paper positions on or off, but changing that rule requires typing a confirmation phrase; the app saves the change, updates the rulebook's identity, refreshes what it shows immediately, and makes older trade proposals unusable so they cannot sneak through under different rules. Tests used a temporary rule file to flip the real button off, on, and off again and confirmed that both the saved setting and the on-screen status changed every time. Stock-suggestion switches now truly stop sources that are turned off. These controls still cannot enable real-money trading, approve an order automatically, or turn a suggestion into a trade, and they do not prove that any stock idea will make money.

## Action Plan Phase 2 hygiene — 2026-08-03 — implementation `34ce463` plus independent review `36c69d3`

**Technical:** Phase 2 closed AP-1, AP-2, and AP-4 with a read-only-by-default `verify_database_schema()` contract and `verify-db-schema` CLI, an explicit `--apply` migration path through the production `AssistantStore` initializer, repository-wide ignoring of machine-local `artifacts/`, and reconciled general-readiness, ML, action-plan, and characterization documentation. Verification builds its reference schema through the real initialization path, requires every declared table and column, compares normalized definitions for every named index and trigger, reports missing and mismatched objects separately, and fails closed on absent databases or weakened enforcement; the independent review proved red that a same-named non-unique active-epoch index and no-op broker-order trigger had incorrectly passed the implementation, then corrected both definition checks. Migration tests preserve legacy rows, the operator database was checked read-only after a verified pre-change backup, and final validation passed 81 focused tests plus 2,441 passed, 1 skipped, and 25 warnings under Python 3.13.14 with clean compilation and diff checks. Table column types and constraints are not compared byte-for-byte, no execution-kernel behavior changed, and no policy, broker, scheduler, evidence epoch, or live authority was modified.

**Plain language:** The project now has a safe inspection command that checks whether its local database contains the pieces the current program expects before evidence collection begins. It only looks unless the owner deliberately asks it to apply upgrades, and it now checks what important safety indexes and triggers actually do instead of trusting their names; the review caught this by replacing two safeguards with weaker look-alikes and proving the original check was fooled. The project also ignores generated models, datasets, and licensed files so scheduled runs do not make the source tree appear modified, and its planning documents now agree about what is finished and what comes next. This is maintenance that makes later paper-operation evidence more dependable; it does not create a strategy, place an order, start evidence collection, or permit real-money trading.

## GR-1D — manual reconciliation behind call-time dependency injection — 2026-08-03 — merge `711095c` plus independent review `2f37210`

**Technical:** GR-1D moved the 221-line `reconcile_submission()` orchestration from `assistant/execution_service.py` into `assistant/execution_kernel/reconcile.py::run_submission_reconciliation()` behind a frozen 13-field `ReconciliationDeps` contract built from the facade namespace on every call. The contract preserves the deferred broker import, exception identity, three status constants, clock, stored-intent parsing, lookup/matching/replacement/absence helpers, and broker-order journal seam; a symtable guard proves the kernel body reads no module-global runtime collaborator. The three-way broker outcome contract, fresh-absence grace and reservation retention, aged-confirmed-absence release, replacement-chain handling, mismatch-triggered persistent kill switch, unexpected-error recovery, and storage-owned atomic transitions are unchanged. PR #120's merge tree is exact, independent review required no code correction, representative facade-bypass and kill-switch-suppression mutations were detected, and the combined final suite passed 2,485 tests with 1 skipped and 25 warnings plus clean compilation and diff checks. The facade is 952 lines, so GR-1 remains partial until GR-1E assesses the 281-line execution composition and recovery wrappers.

**Plain language:** When an order submission gets stuck in an uncertain state, the assistant has to ask the broker what really happened without guessing: it keeps money reserved while the answer is unclear, releases it only after the broker reliably confirms there was no order, and stops the platform if an order under the same key does not match what was approved. That careful recovery procedure now lives in its own clearly named module instead of the giant outer service file. The outer service hands it a fresh toolbox on every call, so tests and operators can still replace the clock, broker lookup, or other helpers exactly as before. Reviewers deliberately broke the toolbox connection and disabled the mismatch emergency stop; the tests caught both. This is safer organization of existing paper-execution plumbing, not a new trading feature, and one final assessment is still needed before the broader kernel split can be called finished.

## Exploratory residual and PEAD-persistence signal utilities — 2026-08-03 — merge `5a6ffd5` plus independent review `6d3603d`

**Technical:** PR #121 added causal residual-momentum and residual-reversal features, volatility-scaled cross-sectional momentum, consecutive-earnings-surprise persistence, and two reporting-only out-of-sample block-bootstrap runners. Rolling regression moments are shifted, benchmark gaps and degenerate volatility fail closed, momentum windows skip the most recent month, PEAD sorts and slices earnings causally, and the runners use next-open entry timing while explicitly labeling yfinance/current-universe data as non-point-in-time. Independent review corrected the full-screen Bonferroni denominator from a stale six cells to one shared eight-cell family, centralized fail-closed confirmation-primary evidence selection, and documented PEAD's long-only direction asymmetry; both reverse mutations were detected. Focused corrected validation passed 138 tests and the combined full suite passed 2,485 tests with 1 skipped and 25 warnings. No result was promoted and no registry, policy, proposal, execution, broker, scheduler, epoch, or live-authority path changed.

**Plain language:** The research toolkit can now test four more ideas: whether company-specific winners keep winning, whether an unusually company-specific one-day move reverses, whether steady momentum is more meaningful in calmer stocks, and whether several earnings beats or misses in a row carry more information than one. The code carefully avoids using future rows, enters on the next open in its reports, and openly warns that free current-day data cannot reproduce exactly what investors knew years ago. The review found that the two reports were using different strictness levels for what was supposed to be one experiment, fixed them to share the stricter eight-part threshold, and made the report stop if its evidence labels ever disappear. These are tools for exploring hypotheses only: they have not shown that any strategy makes money and cannot create or execute a trade.

## GR-1 execution-kernel split — 2026-08-03 — GR-1E merge `2f4a360` plus independent review `8bbe82b`

**Technical:** GR-1 is complete against the intended scope of archived plan sections 6.2–6.4: the stable `assistant.execution_service` compatibility facade now composes eight independently testable execution-kernel modules for claims, revalidation, submission, outcomes, errors, intents, proposal validation, and reconciliation while atomic claim/reclaim and multi-row transition semantics remain in `AssistantStore`. GR-1E independently assessed the 281-line `execute_approved_paper_proposal()` residue and accepted it as the necessary coordinator: it sequences extracted phases, maps exceptions and terminal outcomes, builds telemetry/diagnostic context, and contains one broker-submission call but no inline financial computation, state-transition SQL, or broker-outcome interpretation. Review corrected overbroad measurement, recovery-call, test-history, and architecture-debt claims without changing runtime code; `allocation_batch.py` cross-leg reservation debt remains open. Final validation passed 93 focused tests and 2,485 full-suite tests with 1 skipped and 25 known warnings under Python 3.13.14, plus clean compilation and diff checks.

**Plain language:** The assistant's most safety-sensitive paper-order workflow is no longer buried in one giant block: its major jobs now live in smaller pieces that can be tested on their own, while the outer service remains the traffic controller that runs those jobs in the correct order. The final review concluded that moving the traffic controller into yet another file would only move complexity, not remove risk, so GR-1 stops here. The database still owns the atomic “only one worker can claim this proposal” safeguards, uncertain broker results still go through reconciliation instead of a blind retry, and no live-trading permission or new trading ability was added. One related batch-allocation design problem remains explicitly tracked for later work; completing this milestone does not mean the whole system is operationally ready or profitable.

## GR-3 fault injection and adversarial drills — 2026-08-03 — implementation `4c395d7` plus independent review `9f5ab5e`

**Technical:** GR-3 supplies an eleven-fault/fourteen-test adversarial matrix using temporary real SQLite stores, real execution/reconciliation entry points, and a scripted broker, plus `scripts/run_fault_drill.py` for complete-inventory execution and atomic hash-stamped reports. The matrix covers ambiguous submit timeouts and duplicate identities, actual `submitting` restart recovery, reconciliation identity anomalies, ticker halts, share-count mismatch, stale/future quotes, journal-write rollback, mid-flight kill switches, and database/credential test isolation. Independent review made skipped or abnormally terminated pytest runs fail closed, bound active-epoch drill rows to the report's exact clean commit, atomically paired reconciliation kill switches with deduplicated critical alerts across manual/startup/stream paths, strengthened no-partial-state assertions, and made report publication atomic and no-overwrite. It adds producers for `ambiguous_submission`, `restart_recovery`, and `kill_switch`; `alert_delivery` still requires GR-5 and `backup_restore` retains its existing producer. Validation passed 110 focused tests and 2,506 full-suite tests with 1 skipped and 25 warnings under Python 3.13.14, plus a post-full seven-test runner check, an 11/11 CLI run, clean compilation, and clean diff checks.

**Plain language:** The project can now rehearse eleven dangerous situations instead of trusting that its emergency code will work: a broker reply disappears, the app restarts while an order may already exist, a broker order does not match what the owner approved, storage fails halfway through a journal update, or the emergency stop turns on while an order is in flight. The drills use a pretend broker but a real temporary database and the real paper-order safety code. Review found that the original runner could mistakenly call a skipped drill successful or attach results from different code to an evidence period, and that a mismatched broker order stopped trading without creating the promised critical alert; all are corrected. These rehearsals prove software failure handling on fixtures, not safe live operation, market profitability, physical disk-exhaustion behavior, or permission to trade real money. A real alert-delivery channel is still a separate owner decision and GR-5 task.

## GR-5 alert delivery — 2026-08-03 — implementation `00a8d13` plus independent review `944001b`

**Technical:** GR-5 adds a dependency-free Windows PowerShell/WinRT desktop-notification channel for immediate critical operational alerts, immutable `alert_deliveries` attempt records, occurrence-based re-delivery, a storage-verified weekly self-test, CLI commands, platform-readiness checks, and the Streamlit Operations dashboard; warning alerts remain batched rather than interrupting the operator. A failed send is preserved as a failed attempt and escalates to a durable critical condition without being re-sent through the known-broken channel. Independent review found and corrected a P2 recovery/readiness gap: that durable channel-failure alert now keeps mandatory readiness failed until a successful later self-test proves recovery and acknowledges it. The review exercised one disposable-database Windows toast successfully, passed 43 focused alert/readiness tests and 56 CLI/UI/import-boundary tests, then passed the full suite at 2,526 passed, 1 skipped, 25 warnings under Python 3.13.14 with clean compilation and diff checks. This adds no broker, proposal, policy, order, scheduler, evidence-epoch, or live-trading authority.

**Plain language:** Critical problems are no longer just entries hidden in a database: the app can put a Windows notification on the operator’s screen and keep a record of each attempt. It will not keep interrupting for the exact same problem, but it will notify again if the problem happens anew; lower-priority warnings wait for the daily briefing. If notifications fail, the app records that clearly and refuses to call itself ready until a later test notification works. The review sent one harmless test notification using a temporary database and checked the whole project’s automated tests. This makes it easier to notice operational problems, but it does not prove a person actually read a notification, install automatic scheduling, make a strategy profitable, or permit real-money trading.

## GR-2 ordered risk-check registry — 2026-08-03 — implementation `03895ae` plus independent review `0167c67`

**Technical:** GR-2 replaces the roughly 600-line hand-written sequence in `validate_trade_intent()` with a frozen, ordered `RISK_CHECK_REGISTRY` of twenty named checks carrying explicit side applicability, terminality, and lifecycle-phase metadata. A shared `_GateContext` preserves the historical dependency order and sanitized Decimal intermediates; the kill switch remains the only current terminal check, buy versus non-buy asymmetry and every violation identity/message remain compatible, and the gate dynamically executes the registry so an injected entry is enforced without driver edits. Independent review compared 1,200 deterministic old/new cases with identical outcomes, corrected terminal handling to stop only when the terminal check itself adds a violation, bound the frozen inventory to each actual runner, and replaced a vacuous violation-code test. The proposal-generation heuristic and batch/pending-order input calculations remain documented, deliberately separate architecture debt. Validation passed 290 focused tests, all 11 fault IDs, and 2,543 full-suite tests with 1 skipped and 26 warnings under Python 3.12.13, plus clean compilation and diff checks. No persistence schema or execution authority changed.

**Plain language:** The assistant already had a long safety checklist before it could send a paper order, but that checklist was written as one giant sequence that was easy to damage when adding or moving a rule. It is now an ordered list of twenty named checks, so reviewers can see every rule and tests loudly notice if one disappears, moves, changes sides, or becomes connected to the wrong piece of code. The answers produced by the new list match the old checklist, including the emergency stop, cash and exposure limits, stale prices, duplicate orders, and earnings blackout. Review found and fixed two future-maintenance traps in how stopping rules and the frozen list were tested. This reorganizes and strengthens the existing paper-trading safety checklist; it does not choose stocks, approve trades, connect to real money, prove profitability, or start the formal evidence period.

## UI-2a/UI-2c sidebar navigation and Buying page — 2026-08-03 — implementation `cbae8e6` plus independent review `3a29138`

**Technical:** The Streamlit UI now replaces its eight-tab container with sidebar radio routing, renders only the selected surface per rerun, keeps the policy-file context visually separate, and renames the user-facing Watchlist surface to Buying while preserving stable internal identifiers. Global AI preferences use durable non-widget session keys, the protected policy editor re-seeds from persisted policy after navigation, and independent review added a narrow persistence whitelist for benign page inputs such as the Buying cart, allocation values, History filters, and suggestion seeds. Approval, override, bulk-submit, order-cancel, and emergency confirmation phrases are intentionally excluded and therefore clear when their page disappears. Review also changed AppTests to select each target page before its first render and added cart-survival plus sensitive-key regression coverage; no database schema, trading policy, proposal lifecycle, execution kernel, broker integration, signal module, or authority boundary changed.

**Plain language:** The app's main sections now live in a menu on the left, and the old Watchlist is called Buying. Moving to another page no longer wipes ordinary work such as the tickers you placed in your Buying cart, but dangerous confirmation phrases still disappear so you must type them again before approving or canceling anything. Tests open every page directly, switch away from a cart and back, and check that safe inputs remain while trading confirmations are never kept by the navigation helper. This makes the paper-trading app easier to move around without giving the menu permission to place trades, change safety rules, or use real money.

## UI-2b History outcome filtering — 2026-08-04 — implementation `335c9fc` plus independent review `9dcff80`

**Technical:** UI-2b adds a canonical seven-group proposal-outcome taxonomy beside the nineteen lifecycle statuses, with an exhaustive frozen mapping that keeps legacy `executed` unresolved, reserves Filled exclusively for `filled`, and sends unknown future values to Other / unknown. A parameterized read-only storage query performs known/unknown outcome filtering before newest-first row limiting; the History page exposes an outcome multiselect, retains exact status as an Advanced intersecting filter, displays active-filter semantics and an Outcome column, and preserves the benign outcome selection across sidebar navigation. Independent review found no runtime defect and closed one P3 test-sensitivity gap with a large-history AppTest that detects a fetch-then-filter reverse mutation. Final validation passed 73 focused tests and 2,576 full-suite tests with 1 skipped and 25 warnings under Python 3.13.14, plus clean compilation and diff checks. No schema, lifecycle, proposal, broker, policy, scheduler, epoch, ML/LLM, or execution-authority behavior changed.

**Plain language:** History can now group technical proposal states into understandable outcomes such as waiting for a decision, still working at the broker, filled, refused, or closed without a fill. Old records that only say the broker accepted an order are never mislabeled as filled, and a status from a newer app version appears as unknown rather than completed. You can combine an outcome with an exact technical status, and the page explains that both conditions must match. Review tested a long history where newer unrelated rows could otherwise hide an older match and confirmed the database filters before applying the display limit. This only changes how existing records are found and shown; it cannot approve, cancel, submit, reconcile, dismiss, or delete anything.

## UI-3 interactive Backtest page — 2026-08-04 — implementation `198339d` plus independent review `540467e`

**Technical:** UI-3 adds a ninth Streamlit page backed by a pure `backtest.interactive` composition layer and the existing walk-forward engine. Its frozen inventory exposes six price-only signals with namespaced parameter widgets, synthetic-default or cached yfinance inputs, whole-universe or basket scope, fixed executable `next_open` entry, multi-horizon summaries, and an explicitly non-portfolio cumulative net-return chart; completed results persist in session state with their actual data coverage, horizons, entry timing, slippage, and research caveats. Independent review corrected fail-open experiment boundaries by rejecting empty provider responses, impossible history/signal combinations, fractional integer coercion, invalid horizons, and negative/non-finite slippage, while visibly retaining partial real-data coverage. Exact-frame engine equivalence and transitive no-authority/no-ML reachability are mutation-tested. Final validation passed 88 focused tests and 2,613 full-suite tests with 1 skipped and 25 known warnings under Python 3.13.14, plus clean compilation and diff checks. No persistence schema, registry, proposal, policy, broker, order, scheduler, evidence-epoch, ML/LLM, or live-authority path changed.

**Plain language:** The app now has a Backtest page where you can choose one of six price-based signals, adjust its settings, and see how it would have behaved over several holding periods. Fake random data is the default and is clearly labeled as a plumbing test; real free-market history is labeled exploratory, warns when tickers or history are missing, and stops instead of pretending that missing data means “no signals.” The review also made the page reject settings that would silently change or make the experiment impossible. The chart is a simple running total of signal returns, not a real portfolio, and nothing on this page can recommend, approve, or place a trade. A promising-looking result still needs the separate frozen command-line significance process and is not proof that the strategy makes money.

## UI-2d durable proposal dismissal/archive — 2026-08-04 — merge `8f2e9a7` plus independent review `a118470`

**Technical:** UI-2d adds the terminal, non-executable `dismissed` proposal status and a shared storage eligibility boundary that permits only pristine `proposed` or `expired` rows with no validation telemetry, broker order/event, execution reservation, allocation-batch reference, or execution-shaped payload evidence. `proposal_dismissal_eligibility()` returns exact refusal reasons and a SHA-256 confirmation hash over the complete durable proposal state; `dismiss_proposals()` rechecks the same rule inside `BEGIN IMMEDIATE`, requires the hash, uses conditional all-or-nothing updates, preserves original metadata on idempotent replay, and records actor, reason, timestamp, and source status without deleting the row or idempotency key. The preview-first CLI requires a reason, hash, and exact confirmation phrase; History hides expired/dismissed rows by default, preserves explicit filters, and offers a typed-confirmation bulk archive flow that clears stale proposal controls. Independent review corrected two P2 fail-closed gaps involving execution telemetry and malformed batch-reference shapes plus one P3 incomplete-hash boundary. Final validation passed 291 focused/adjacent tests and 2,666 full-suite tests with 1 skipped and 25 warnings under Python 3.13.14, with clean compilation and diff checks. No schema migration, broker call, order mutation, policy change, evidence epoch, ML/LLM authority, or live-trading path was added.

**Plain language:** History can now hide old paper-trade ideas you never used without erasing them. You select unused proposals, explain why you are dismissing them, and type an exact confirmation; the app then marks them archived while keeping their full record and unique identity so they cannot be mistaken for deleted evidence or silently created again. The safety checks refuse anything that was validated, grouped into a buying batch, reserved, sent toward the broker, or otherwise touched execution, even when stored data is damaged. Review found three ways those checks or the confirmation fingerprint were not strict enough and fixed all three. This feature only declutters unused paper proposals: it cannot cancel an order, delete trading history, approve a trade, enable real-money trading, or automatically expire old proposals.

## GR-4 — data-layer resilience and honesty — 2026-08-05 — implementation `3fa4229` / `eb33aa9` / `a4f09e3` plus independent review `7eef1c5` / `69d71a3`

**Technical:** GR-4 introduces a declared `PriceSource` boundary and append-only `data_provider_fetches` evidence used by GR-0's `data_integrity` dimension. Yfinance declares `provides_point_in_time_lineage=False`; recorded fetches store requested/returned/missing counts, timezone-aware fetch time, latest bar session, and strict boolean lineage. NYSE-calendar freshness requires the latest completed session and accepts a current-date bar only during a real open session. The DecisionPacket carries expected-session and freshness fields; Briefing renders a `DATA DEGRADED` banner even when history is too short for trend; strategy proposals refuse missing or stale bars while risk-reduction proposals remain bar-independent; and newly generated proposals bind exact proposal-time shares so pre-submit validation can refuse forward or reverse split-shaped drift before broker preflight. Independent review corrected seven P2 fail-open edges (non-session freshness, assertion-shaped evidence, coerced readiness booleans, missing banner on short stale history, silent empty strategy results, unrecorded strategy fetches, and incomplete forward-split refusal), extracted an import-boundary-safe share-reconciliation helper, and corrected the separate exploratory dip-grid script so underfilled horizons never enter labeled statistics. Final validation is recorded in `docs/REVIEW_2026-08-05_GR4_DATA_HONESTY.md` and `docs/SESSION_HANDOFF.md`. No live authority, model promotion, or mid-epoch operational deployment was introduced.

**Plain language:** The app now keeps an honest diary of market-data downloads and uses that diary—not a caller saying “trust me”—to decide whether prices look fresh and trustworthy. If the free data feed is late, empty, or only available outside a real trading session, the Briefing shows a clear degraded-data warning, strategy rebalance ideas stop instead of pretending there is nothing to do, and ordinary risk-reduction ideas still work because they do not need those bars. New trade ideas also remember exactly how many shares were held when they were created, so a stock split that changes the share count cannot sneak past the final safety check. Review found and fixed several ways bad or missing data could look acceptable. A separate research script about buying sharp dips remains exploratory only and is not permission to trade.

## Investment committee replay corpus and CLI — 2026-08-05 — implementation `21379b4` plus independent review `eaecadf`

**Technical:** The advisory investment-committee boundary now has a 69-case deterministic release corpus—51 replay, 10 injection, and 8 memory-poisoning cases—executed through the real projection, provider orchestration, strict schema, grounding validator, and error mapper without network access. Inventory minimums and a canonical SHA-256 freeze the reviewed corpus content, including one explicitly measured lexical-filter limitation. The `committee-review <proposal-id>` CLI composes the existing percentages-only packet projection, Anthropic provider, and mandatory `ai_runs` audit wrapper; all defined gate, packet, projection, provider, schema, validation, and audit failures return one sanitized `Review unavailable (<code>)` line with exit 2, while accepted output shows every cited section and repeats the human-approval/revalidation requirement. Independent review corrected packet-build exception handling and count-only corpus freezing; validation covered committee UI/provider/service compatibility and the transitive ML/authority boundary. The experiment environment gate remains enabled, proposal rows are read-only, and no policy, broker, execution, or schema contract changed.

**Plain language:** The optional AI committee now has a large, fixed exam that checks whether it rejects invented facts, hidden warnings, fake citations, instruction attacks, and poisoned saved text. The command-line app can ask the committee to explain one existing risk-reducing paper proposal, but it gives a clear unavailable message if the needed data, provider, validation, or audit record fails instead of showing a traceback or half-finished answer. Review made sure the exam itself cannot be quietly weakened just by keeping the same case names, and made broker/data outages use the same safe unavailable screen. This remains an explanation tool behind an explicit experimental switch: it cannot create or change a trade, approve an order, contact the broker to execute anything, or remove the owner's required approval and deterministic safety checks.

## GR-7a — annual realized-gain tax reporting — 2026-08-05 — implementation `7dd55b6` / `365bb11` plus independent review `6221f6d`

**Technical:** GR-7a adds a pure reporting layer (`assistant/tax_reporting.py`) over the existing tax-lot ledger: realized components are replayed from journal fills plus journal-confirmed corporate actions, money is Decimal with per-share multiplication rather than float-product conversion, tax years and exported timestamps use America/New_York, and wash-sale rows remain advisory flags with unadjusted basis. Coverage status is embedded in CSV/JSON artifacts; only live `source="alpaca"` snapshots may verify completeness, while sample/manual portfolios and broker outages stay explicitly unverified. The CLI `tax-report` command exits 2 on incomplete/unverified coverage while still writing the artifact and keeps stdout as a pure machine artifact; the Streamlit Reports page builds on demand without recording provider fetches and refuses to display a report whose year disagrees with the picker. Independent review corrected sample-as-broker verification, float-product cents, stdout pollution, non-read-only packet loading, year desync, and coverage freeze/outage honesty. Final validation: 2,840 passed / 1 skipped / 25 warnings. No proposal, approval, execution, schema, or live-authority path changed.

**Plain language:** The app can now export a year of realized gains and losses from its own trade records into CSV or JSON that an accountant can open. The file says up front whether the share history was checked against the live broker, left unchecked, or found incomplete, and it never pretends the demo sample portfolio is the broker. Wash-sale hints are warnings only; the app does not change cost basis. Review fixed several ways the export could look more trustworthy than it was. This is a reconciliation aid, not tax advice and not a substitute for the broker's official tax forms, and it cannot place or approve any trade.

## GR-7b — idle-cash / mandate reporting — 2026-08-06 — implementation `e25aa42` plus independent review on `user/grok/review-gr7b-idle-cash-20260806`

**Technical:** GR-7b adds pure `assistant/cash_reporting.py::evaluate_idle_cash` that measures portfolio cash against `TradingPolicy.min_cash_reserve_pct` / `max_total_exposure_pct` (signed reserve surplus, unused exposure capacity, binding constraint, and non-suggestive `policy_headroom`) and against the mandate's annualized volatility band via descriptive `required_invested_volatility_pct ≈ floor / invested_fraction`, including the figure required even at the policy exposure ceiling. Unmeasured volatility is absent, never zeroed; non-positive or non-finite equity and unusable measured volatility refuse via `CashReportError`. CLI `idle-cash` and the Reports expander consume an Alpaca or sample portfolio snapshot without calling decision-packet builders that record GR-4 provider fetches. Independent review closed provider-fetch writes on CLI/UI, `ValueError` escapes from NaN measured vol, and acceptance of negative measured vol. Final validation: 2917 passed / 0 skipped / 25 warnings. No proposal, approval, execution, schema, or live-authority path changed; nothing deployed mid-epoch.

**Plain language:** The app now shows when most of the account is sitting in cash even though no rule is being broken — previously only cash *below* the reserve floor was visible. It also translates that idle cash into how volatile the invested holdings would need to be to hit the mandate's target risk, and whether the policy's own invest-at-most limit makes that target unreachable no matter how much cash you deploy. Review made sure opening the report does not secretly write market-data evidence and that bad volatility inputs fail cleanly. This is a read-only dashboard: it never suggests a trade and cannot place one.

## GR-7c — performance attribution (cash drag + residual) — 2026-08-06 / follow-up review 2026-08-07 — implementation `1da4154` plus reviews on `user/grok/review-gr7c-attribution-20260806` and `user/grok/review-gr7c-weight-bias-20260807`

**Technical:** GR-7c adds pure `assistant/attribution.py::evaluate_attribution` over recorded `portfolio_equity_snapshots`: portfolio and SPY time-weighted returns, beginning-of-period invested weights equalized by market session (not capture frequency), allocation `(w-1)*R_benchmark`, and selection as the residual of active return, with cost/tax reported as already inside the equity curve and never re-deducted. Sector Brinson is undefined without mandate target weights; the single bucket matches the SPY ticker paper-evidence already binds. Sufficiency counts independent market sessions from recorded `session_date`, not UTC-derived dates or intraday re-captures. Snapshot `total_equity` is post-flow; TWR uses `value_before_flow = total_equity - flow`, matching `portfolio_performance_report`. CLI `attribution` is account-scoped and snapshot-only (no provider fetches), refuses skips that would drop a non-zero external flow, and discloses the weight method in JSON and human output. Independent review closed NaN cost escapes, cash>equity clamping, read-only proof, argparse honesty, overinvested labelling, deposit-as-return wiring (+33.3333% on a pure deposit series), weight-method disclosure, and human-CLI cash-drag hardcoding. Final validation after follow-up review: 2955 passed / 0 skipped / 25 warnings. No proposal, approval, execution, or live-authority path changed; nothing deployed mid-epoch.

**Plain language:** The app can now explain how much of the paper portfolio’s under- or over-performance versus SPY came from simply holding a lot of cash, and how much is everything else (labelled a leftover, not “skill”). It refuses to pretend many same-day snapshots are many days of evidence, equalizes busy capture days with quiet ones when measuring how invested the account was, and treats bank deposits as deposits rather than investment gains. Review fixed ways bad money numbers, skipped rows, or post-deposit equity could be misread. This is a read-only report from saved account history: it cannot place a trade, and it is not a claim that any leftover return is skillful stock picking.

## QC-1 — QuantConnect research client (results-only transport) — 2026-08-07 — implementation `ba8ae6d` plus independent review on `user/grok/review-qc1-api-client-20260807`

**Technical:** QC-1 adds `research/quantconnect.py`, an offline-testable QuantConnect cloud client that can only call an allowlisted set of results/project/compile/backtest/optimization endpoints. Authentication follows QuantConnect’s documented time-bound SHA-256 hash so the API token is never transmitted; credentials load from `QC_USER_ID` / `QC_API_TOKEN` only; `__repr__` redacts the token. Independent review forced every call to HTTP POST (including `authenticate` with `{}`), hardened the allowlist against prefix and `../` bypasses, required in-band `success is True`, and rejected bool/blank ids and bad timeouts. AST tests pin that the module does not import assistant/execution/risk/ml/signals/strategies. Final validation: 3008 passed / 0 skipped / 25 warnings. No live QC call was made (credentials absent); no proposal, approval, execution, or live-authority path changed; nothing deployed mid-epoch. Look-counting over QC runs is a later milestone.

**Plain language:** The project can now talk to QuantConnect’s cloud to run and read *its own* backtest results, without a way to pull QuantConnect’s market data into local spreadsheets (their licence forbids that). Review fixed the first-call footguns: use POST the way QuantConnect documents, and keep the “no market data” door locked even against tricky path strings. This is research plumbing only — it cannot place a trade, and it is not yet the system that counts how many strategy ideas have been tried.
