# Codex exhaustive line-by-line codebase review — 2026-08-07

Audience: repository owner, Claude, Grok, Codex, and future reviewers.

Status: **COMPLETE. Findings only; no runtime correction is authorized or
included in this review.**

> **Correction status (2026-08-08):** The owner subsequently authorized all
> corrections. This document preserves each issue exactly as discovered at
> baseline. The ordered dispositions, fixes, regression evidence, and final
> validation are recorded in
> `docs/REVIEW_2026-08-08_COMBINED_SCAN_FIX_LEDGER.md`; all 24 CXL findings are
> fixed there.

## 1. Scope and baseline

- Review baseline: `a48bb85241b335e65fada1c6005a60f99b772bae` (`main`).
- Inventory: 199 production Python modules, 156 test Python modules, and four
  PowerShell operational modules.
- Documentation context: all 60 Markdown documents under `docs/`, root
  `README.md`, `CLAUDE.md`, and `AGENTS.md` were read before this source pass.
- Operational boundary: `paper-epoch-002` is running on a separate computer
  using its frozen stable application instance. This development checkout is
  not the epoch runtime, and nothing in this review is deployment evidence.
- Method: every module is read line by line; mechanical AST/search passes are
  supporting tools, not substitutes for that reading. A finding is recorded
  only after an executable reproduction or a complete source-path proof.

Review result: **0 P0, 0 P1, 19 P2, and 5 P3 findings.** All 24 remain open
pending the owner's assessment. A passing test suite does not close a source-
proved finding whose dangerous direction is absent from the tests.

## 2. Issue ledger

| ID | Priority | Status | Commit | Location | Issue and impact | Evidence | Reason for fix | Correction | Verification |
|---|---|---|---|---|---|---|---|---|---|
| CXL-001 | P2 | Open | `a48bb85` | `assistant/tax_lots.py:234-269,613-625`; `tests/test_tax_lots.py:197-223` | FCS-016's correction makes a leap-day acquisition become long-term one day late. Stock acquired 2024-02-29 and sold 2025-03-01 is reported short-term; the code waits until 2025-03-02. This flows into the accountant-facing tax report and proposal tax-lot advisory. | Executable reproduction returned False on 2025-03-01 and True on 2025-03-02. IRS Publication 550 says counting begins the day after acquisition and includes the disposition date; the current test explicitly pins the contrary leap-day result. | GR-7a must not publish the wrong holding-period class, and the advisory must not overstate tax or delay a risk-reducing sale by one day. | Pending owner instruction. | Required: corrected leap-day boundary tests across local/UTC times plus report/advisory propagation. |
| CXL-002 | P2 | Open | `a48bb85` | `assistant/cash_reporting.py:109-148,258-275`; `assistant/portfolio_analytics.py:10-48` | FCS-004 still claims execution-equivalent policy headroom without reading `buying_power`; it also claims commitment completeness when open orders are unavailable, counts the original rather than remaining quantity on partial fills, and lets a negative notional increase headroom. | With equity 10,000, cash 9,000, buying power 1,000, and a 1,000 reserve, the report returned 4,000 headroom while the gate's `min(cash,buying_power)` cash bound permits 0. With `open_orders_available=False`, it still returned `committed_capital_complete=True`. | This fails GR-7b's definition of done and gives the owner a materially incorrect statement of what policy permits. | Pending owner instruction. | Required: parity matrix against the execution gate for buying power, unavailable orders, partial fills, malformed values, and unknown market-order values. |
| CXL-003 | P2 | Open | `a48bb85` | `assistant/context_builder.py:417-465`; `data/event_data.py:31-53`; `data/corporate_actions.py:48-61`; `scripts/personal_assistant_ui.py:650-664,2703-2724` | An unexpected failure in optional live earnings or dividend enrichment propagates before the Propose & Approve page computes deterministic risk-reduction proposals. Enabling the optional checkbox can therefore suppress already-available risk-reducing sells. | Replacing `fetch_upcoming_earnings` with a function raising `RuntimeError("provider down")` made `get_upcoming_events(..., fetch_live=True)` propagate the exception. The UI calls `_load_packet` before `generate_risk_reduction_proposals`. | `CLAUDE.md` explicitly forbids an optional feature failure from obstructing risk reduction. | Pending owner instruction. | Required: provider-failure tests at the context-builder and rendered-page boundaries proving unavailable event records while risk-reduction proposals remain visible. |
| CXL-004 | P2 | Open | `a48bb85` | `assistant/policy.py:247-314`; `scripts/personal_assistant_ui.py:4116-4133`; `tests/test_policy_persistence.py:96-133` | FCS-015's UUID temporary file prevents scratch-file collision but not the stated lost-update race. Two stale browser tabs can save different changes with the same bumped version; the later writer silently restores an authoritative flag changed by the first. | Starting from `(allow_new_positions=True, strategy=False, v)`, tab A saved `(False,False,v.1)`, then stale tab B saved `(True,True,v.1)`; the final policy was `(True,True,v.1)`. Existing tests do not exercise concurrency. | Authoritative durable policy must detect stale writes; silently re-enabling new positions is a material state-integrity failure even though later order approval remains required. | Pending owner instruction. | Required: deterministic stale-writer/concurrent-writer test with expected fingerprint/version compare-and-swap semantics. |
| CXL-005 | P3 | Open | `a48bb85` | `docs/ACTION_PLAN_2026-08-02.md:57-60,162-163,289-290,319,341`; `docs/GENERAL_READINESS_STATUS.md:920`; `docs/OPERATIONS_RUNBOOK.md:369`; `docs/REVIEW_2026-08-07_FULL_CODEBASE_SWEEP.md:16-30,246-252` | Active documents contradict current epoch, UI, readiness, alert-delivery, and FCS-fix state. The go-to sequencing authority simultaneously calls epoch 1 active and closed, and says dismissed proposals and the Backtest tab do not exist although later sections record them complete. | Direct contradictory statements in the cited current documents. | Inaccurate active documentation can mis-sequence future agents and mislead operations; dated historical facts should remain historical, but current-state sections must agree. | Pending owner instruction. | Required: cross-document state check against `SESSION_HANDOFF.md` and the accepted milestone records. |
| CXL-006 | P3 | Open | `a48bb85` | FCS-012/FCS-013 changes; `tests/test_run_personal_assistant_cli.py`; `tests/test_tax_reporting.py`; FCS-015 tests above | Several Claude fixes cite the existing suite even though it is insensitive to the changed behavior. There is no fix-specific proof that `list --limit` rejects non-positive values, that `tax-report --year` uses the new parser guard, or that interrupted tax-artifact publication preserves the old destination. The policy tests passed with the old shared temporary name and do not exercise writers. | Source/test cross-check: the affected parser arguments and atomic helper have no dangerous-direction regression; reverse mutation of those call sites is not targeted by the cited tests. | `CLAUDE.md` requires every defect fix to have a regression test that fails without it, including concurrency and crash directions. | Pending owner instruction. | Required: red/green parser, artifact-failure, and writer-race tests. |
| CXL-007 | P2 | Open | `a48bb85` | `assistant/order_lifecycle.py:78-112`; `assistant/storage.py:3391-3444` | A delayed partial-fill event with the same cumulative filled quantity can move a proposal backward from `cancel_pending` to `partially_filled`. The status allow-list permits that transition and the monotonic quantity check uses `>=`, so an equal-quantity replay is projected even though it contains no new fill. | Executable reproduction projected `partially_filled(4)` -> `cancel_pending(4)` -> delayed `partially_filled(4)` and returned `broker_event_projected=True`; the final durable status was `partially_filled`. Existing monotonicity coverage tests only a lower-quantity event after `filled`. | Losing the durable cancellation-pending state misstates the broker lifecycle and can cause repeated cancellation handling or incorrect operator decisions from a stale event. | Pending owner instruction. | Required: out-of-order/equal-quantity transition matrix, including `cancel_pending`, with status and event-time monotonicity. |
| CXL-008 | P2 | Open | `a48bb85` | `assistant/storage.py:4110-4180`; `assistant/portfolio_ledger.py:172-184` | Ledger idempotency treats every repeated `external_id` as a safe no-op without verifying that the repeated transaction has identical content. A corrected or conflicting cash transfer, dividend, or fill can therefore be silently discarded while the caller receives the same `False` result as an exact retry. | Executable reproduction recorded `bank-1` as a $500 deposit, then submitted the same external ID as a $5,000 corrected deposit. The second call returned `False` without error and the authoritative cash balance remained $500. Existing coverage tests only an identical replay. | An idempotency key collision with different financial content is a data-integrity conflict, not success; silently retaining the wrong transaction corrupts cash, holdings, P&L, and reconciliation. | Pending owner instruction. | Required: compare the complete canonical transaction and postings on conflict; exact replay is a no-op, any content mismatch raises and is tested for every journal source. |
| CXL-009 | P2 | Open | `a48bb85` | `assistant/storage.py:4836-4911`; tax-lot/ledger consumers of `list_fills()` | If an order has even one incremental stream fill, `list_fills()` discards every cumulative broker snapshot for that order. When the stream captures an early partial fill but disconnects before later fills and polling subsequently records the final cumulative quantity, the missing remainder silently disappears from the reconstructed ledger. | Executable reproduction journaled an incremental 4-share fill followed by a poll-only cumulative 10-share fill. `list_fills()` returned one 4-share fill and omitted the other 6 shares. | Stream delivery is not guaranteed to cover every execution; silently undercounting fills corrupts holdings, cost basis, realized P&L, and accountant-facing tax output. | Pending owner instruction. | Required: mixed incremental/cumulative gap tests; reconcile cumulative quantity against summed incrementals and represent or explicitly refuse unresolved remainder/basis rather than dropping it. |
| CXL-010 | P2 | Open | `a48bb85` | `assistant/portfolio_ledger.py:230-315`; `assistant/storage.py:4110-4180,3902-3917` | Opening-ledger bootstrap checks for an empty journal and bootstrap marker outside the transaction that appends the opening entry, then writes the marker in a second transaction. Two simultaneous bootstraps can both succeed and double the account; a crash after the journal insert but before the marker also leaves a non-empty ledger that the retry path refuses to repair. | A barrier-controlled two-thread reproduction submitted $100 and $200 opening snapshots. Both calls returned success; the durable ledger contained two opening transactions and $300 cash, with only one last-writer bootstrap marker. | The opening balance is the root of every later cash, basis, and reconciliation calculation and must be exactly-once with its durable marker. | Pending owner instruction. | Required: one transaction/unique sentinel covering empty-ledger proof, opening postings, and bootstrap metadata; concurrent-writer and crash-injection regression tests. |
| CXL-011 | P2 | Open | `a48bb85` | `assistant/tax_lots.py:383-392,439-469`; `assistant/schemas.py:90-105` | A loss sale can be falsely marked as a suspected wash sale when callers omit `Fill.fill_id`, even if there was no replacement purchase. The ledger gives the buy lot a generated ID such as `lot-1`, but wash-sale detection compares the original buy fill's still-empty ID against disposed lot IDs, so the original acquisition is mistaken for a separate replacement buy inside the 61-day window. | Executable reproduction used a one-share buy at $100 followed one day later by a complete one-share sale at $90, with both optional fill IDs omitted. The only realized component returned `wash_sale_suspected=True`; existing wash-sale tests supply explicit IDs and do not cover the schema default. | A false wash-sale warning makes the accountant-facing report overstate tax complexity and can mislead the proposal advisory even though no replacement shares exist. | Pending owner instruction. | Required: use one normalized fill identity consistently during lot construction and wash-sale matching; cover omitted, duplicate, and explicit fill IDs plus genuine pre-sale and post-sale replacement purchases. |
| CXL-012 | P3 | Open | `a48bb85` | `signals/scanner.py:23-57,88-103`; shared callers in `signals/breakout.py`, `signals/relative.py`, `signals/vix_spike.py`, `signals/credit_spread.py`, and `signals/yield_curve.py` | The shared feature calculation divides by a zero rolling standard deviation without replacing the result, and its consumers reject only `NaN`, not infinity. A flat trailing return or volume series followed by one changed observation therefore becomes an infinite z-score and is accepted as the strongest possible signal instead of being treated as unavailable. | Executable reproduction with 20 flat prior returns/volumes followed by one changed row produced `return_zscore=-inf` and `volume_zscore=inf`; the otherwise equivalent residual-signal implementation already uses `.where(std > 0)` specifically to fail closed. | This is a low-probability provider/malformed-series edge on research and display surfaces, but fabricated infinite-strength signals violate the repository's non-finite data discipline and can mislead paper-trading decisions. | Pending owner instruction. | Required: finite-safe denominators and regression tests for flat return, flat volume, and signed macro-proxy windows across every shared scanner path. |
| CXL-013 | P2 | Open | `a48bb85` | `strategies/decline_grid.py:295-316,346-366` | The decline-grid buy-and-hold comparator always values its exit at the row's open because `exit_idx < len(df)` is necessarily true for every valid index. Episodes forced to liquidate on the final available row are valued at that row's close, so the strategy and comparator use different exit prices and the reported edge is not an apples-to-apples result. | Executable reproduction with entry open $100 and final-row open/close of $50/$200 returned a -50% buy-and-hold result although the documented final-close liquidation is +100%. No test references `simulate_buy_and_hold`. | The comparator is the basis for deciding whether the grid adds value over simply holding; a mismatched exit can reverse that conclusion and invalidate the recorded research result. | Pending owner instruction. | Required: pass the actual episode exit-price convention (next open versus terminal close) explicitly and test every outcome branch, including final-row stop/max-hold and forced end. |
| CXL-014 | P2 | Open | `a48bb85` | `ml/artifacts.py:46-75`; contrast `backtest/research_report.py:409-436` | ML artifact and manifest publication claims immutable concurrent-writer safety, but its second `exists()` check is still separated from `os.replace()`. Two writers of different bytes can both pass the check, and `os.replace` lets the later writer silently overwrite the first versioned artifact. | A barrier-controlled two-thread reproduction forced both writers past the final check before replacement; both returned success and the destination contained only one writer's bytes. The research-report writer already documents and fixes this exact TOCTOU class with create-exclusive `os.link`. | Model artifacts are executable serialized objects whose hashes and versioned identities underpin shadow evidence. Silent replacement can detach an artifact from the manifest/evidence that originally named it and destroys the advertised immutable audit trail. | Pending owner instruction. | Required: publish with an atomic create-exclusive primitive and test identical/different concurrent writers plus cleanup and interrupted-write directions for model and manifest files. |
| CXL-015 | P2 | Open | `a48bb85` | `ml/databento_source.py:508-528,559-633,636-797`; consumers in `ml/databento_authoritative.py:502-528,654-661` | The shared Databento `write_immutable_bytes` helper has the same check-then-`os.replace` race: concurrent writers can both pass the final existence check and the later replace silently overwrites an allegedly immutable paid-data snapshot or sidecar. Snapshot names can intentionally coincide when a caller supplies the same `observed_at`, and several content-addressed authority artifacts also reuse this helper. | Complete source-path proof: both existence checks occur before an unconditional replacing rename, and `os.replace` overwrites an existing destination on supported platforms. The structurally identical helper in CXL-014 was reproduced with a barrier and two successful conflicting writers. | Raw paid bytes, rejection/validation manifests, historical-universe snapshots, and authoritative feature batches are the audit evidence for point-in-time claims. Overwriting one under a stable name can mix lineage and content or destroy purchased evidence. | Pending owner instruction. | Required: consolidate all immutable publication on one atomic create-exclusive primitive; test concurrent raw/manifest, content-addressed batch, historical-universe, and failed-validation publication paths. |
| CXL-016 | P2 | Open | `a48bb85` | `ml/datasets.py:560-590,595-690` | Dataset publication repeats the same check-then-`os.replace` race. Two processes saving different feature, label, sidecar, or manifest bytes under the same `dataset_id` can both pass preflight and the helper's final existence check, after which the later replacing rename silently overwrites the first dataset member. Because members are published one at a time, concurrent saves can also leave a mixed set assembled from different writers. | Complete source-path proof: `_preflight_immutable_writes` and both checks in `_atomic_write_bytes` precede unconditional `os.replace`; there is no create-exclusive destination claim or directory-level transaction. The structurally identical helper in CXL-014 was reproduced with two conflicting writers that both returned success. | Dataset hashes, point-in-time sidecars, and manifests are the evidence foundation for every ML experiment. Silent replacement or a cross-writer mixed dataset invalidates lineage and can make later evaluations non-reproducible. | Pending owner instruction. | Required: create-exclusive publication plus an atomic dataset-set ownership/commit protocol; test identical and conflicting concurrent saves, mixed-member interleavings, partial failures, and cleanup. |
| CXL-017 | P2 | Open | `a48bb85` | `ml/experiments.py:188-224`; `ml/research_orchestration.py:70-119,203-248` | Experiment outputs, spec/review artifacts, and content-addressed research datasets also use replacing renames for files documented as immutable. The orchestration byte writer is the least protected variant: after its initial existence check it performs `os.replace` without even rechecking. Concurrent differing writers can therefore overwrite one another, and multi-file dataset materialization can be left as a cross-writer mixture. | Complete source-path proof: all cited publication paths end in `os.replace` rather than a create-exclusive destination claim; experiment and JSON helpers retain the same TOCTOU window as CXL-014, while `_write_immutable_bytes` has an unconditional replacement after temporary-file creation. | These files are the preregistration, human-review, discovery/confirmation, and result evidence used to decide whether research advances. Mutable evidence under a stable hash or experiment identity defeats the independent-review and reproducibility boundary. | Pending owner instruction. | Required: route every ML evidence writer through one reviewed create-exclusive primitive and add conflicting concurrent-writer plus multi-file partial-publication tests for experiment and campaign artifacts. |
| CXL-018 | P2 | Open | `a48bb85` | `ml/monitoring_reports.py:232-288,467-496,786-915`; `ml/monitoring.py:331-409`; `ml/evidence_operations.py:261-314` | Duplicate shadow predictions are detected but do not become a monitoring blocker. Coverage uses the raw prediction-row count, so duplicates can raise coverage above 100%; the final blocker logic checks epoch changes, clock errors, and invalid hashes but ignores `duplicate_generation_count`. Duplicate outcome IDs are likewise collapsed last-writer-wins in `_matured_rows`, and the operations supervisor accepts duplicate subject attempts when the run's declared raw count matches the stored raw count. | Complete source-path proof: `_coverage` computes `len(predictions) / expected`; `lineage_consistency` reports duplicates; `build_epoch_monitoring_report` never reads that duplicate count when assembling blockers; `_matured_rows` constructs an overwriting dictionary keyed by prediction ID; and the supervisor compares subject sets plus declared/stored raw counts without enforcing one row per expected subject. | Shadow evidence is counted toward a future owner promotion review. Repeated rows or conflicting outcomes must never manufacture coverage, sample size, or favorable matured evidence. | Pending owner instruction. | Required: enforce unique expected `(run/date, subject, model, horizon)` attempts and unique identical outcome IDs before aggregation; calculate bounded coverage from unique expected identities and block on every duplicate/conflict in both monitoring and supervision. |
| CXL-019 | P2 | Open | `a48bb85` | `ml/monitoring_reports.py:770-783,786-915` | Operational incident accounting treats the mere existence of any alert as proof that every failed shadow run is tracked. Alerts are not matched to run IDs, epochs, or failure events; one unrelated or old closed alert sets `untracked_failed_run_count` to zero and can make operations `clean` even with many failed runs. | Complete source-path proof: `_operations` uses `len(failures) if failures and not alerts else 0` and `not failures or bool(alerts)`; it never joins an alert to a failure. The final report trusts this `clean` boolean when deciding whether to add the operational-incident blocker. | Failed scheduled evidence runs must remain explicit and attributable. Treating an unrelated alert as coverage can hide missing predictions/outcomes and let an incomplete epoch appear operationally clean. | Pending owner instruction. | Required: use a durable run/incident identity link, require every failure to have a matching incident record, keep unresolved matching incidents blocking, and test unrelated, closed, duplicate, and cross-epoch alerts. |
| CXL-020 | P2 | Open | `a48bb85` | producer `ml/shadow_runtime.py:874-991`; consumers `ml/monitoring_reports.py:613-692`; `ml/presentation.py:424-473,603-613` | The prospective uncertainty contract drifted between the shadow producer and both consumers. The producer records `prediction_interval_daily_pct` and the calibrated ceiling-probability field under `values` (and in `prospective_contract`), while monitoring reads those fields only from `uncertainty`; presentation also reads `uncertainty` and asks for the unrelated key `probability_above_ceiling`. As a result, real prospectively recorded uncertainty is reported as not measured/unavailable and creates permanent blockers. | Complete source-path proof: the available `PredictionRecord` has an `uncertainty` mapping containing only status/hashes/calibration status, while its interval and dynamically labeled probability are in `values`; neither consumer reads those producer locations. | Operators need an honest view of what was prospectively captured, and promotion review must distinguish genuinely missing calibration from a reader/schema mismatch. Permanent false blockers also make the implemented prospective-profile feature unusable. | Pending owner instruction. | Required: define one versioned typed storage schema and make producer, monitor, and presentation consume the same fields; add a round-trip test from `build_volatility_prediction` through storage-shaped data into both reports. |
| CXL-021 | P2 | Open | `a48bb85` | `ml/portfolio_volatility.py:294-411`; `tests/test_ml_portfolio_volatility.py:152-211` | Frozen-weight target construction does not require the price base row to equal `as_of_session`. It selects the last common row at or before that date and then labels the result with the requested as-of date. If every held ticker is missing the as-of row (or the caller supplies a weekend), a stale earlier close is silently used as the return base and the horizon is shifted/mislabeled. Existing alignment tests cover a missing value inside the selected window, not wholesale absence of the as-of session. | Complete source-path proof: `base = frame[frame.index <= as_of_timestamp]` is checked only for emptiness, then `base.iloc[[-1]]` is used without comparing its index to `as_of_timestamp`; `_parse_session` validates calendar format but not exchange-session membership. | A supervised target with the wrong starting session corrupts training/evaluation while retaining plausible values and a misleading identity hash. Point-in-time portfolio research must refuse stale bases rather than silently changing the target window. | Pending owner instruction. | Required: require an exact common as-of price row (and canonical exchange-session sequence for the forward window) and test missing-all-tickers, weekend/holiday, and irregular-session inputs. |
| CXL-022 | P3 | Open | `a48bb85` | `scripts/run_ml_shadow.py:1007-1012,1041-1046,1109-1114`; `scripts/run_personal_assistant.py:432-440,527-535,1187-1209,2494-2510`; `assistant/storage.py:328-360` | The ML `status` command and several personal-assistant inspection commands are described as read-only after dispatch, but both CLIs construct `AssistantStore` before reaching those handlers (the assistant CLI exempts only `verify-db-schema`). Store construction creates the parent directory, opens/creates the database, enables WAL, and runs the entire schema initialization/migration path. Even an invalid ML config therefore mutates or creates the operator-selected database before `status` can report the input error; assistant `list`, `briefing`, `readiness`, and report surfaces share the initialization side effect. | Executable reproduction invoked ML `status` with a nonexistent config and a nonexistent temporary database path; the command failed, but left a newly created 364,544-byte SQLite database. Complete assistant-CLI source-path proof shows store construction at common dispatch before the cited read-only handlers. | A command explicitly advertised as safe read-only inspection should not create or migrate durable state, especially when its actual input is invalid. This is an operational-contract defect rather than a trading-authority failure. | Pending owner instruction. | Required: a genuinely read-only store/open mode (including no parent creation, WAL/schema migration, heartbeat, or alert writes) and tests against nonexistent, old-schema, and read-only filesystem databases across both CLIs. |
| CXL-023 | P3 | Open | `a48bb85` | `scripts/setup_operational_host.ps1:127-157`; `scripts/personal_assistant_ui.py:4193-4214` | The generated operational launcher deliberately reloads user-scope Alpaca and Anthropic credentials so a long-lived shell cannot pass stale values, but it omits the app's supported `FINNHUB_API_KEY` and `DATABENTO_API_KEY`. After either key is added or rotated at user scope, launching from an already-open shell can still make the UI report that provider as unconfigured until the shell itself is restarted. | Complete source-path proof: the generated launcher loops over exactly three environment-variable names, while the UI checks the two omitted provider names from the child process environment. The adjacent launcher comment states the same stale-inheritance failure that motivated adding Anthropic. | The host bootstrap claims to give each app launch the current machine credentials. Omitting two supported sources makes configured functionality appear broken and repeats the already-documented Anthropic incident for Finnhub or Databento. | Pending owner instruction. | Required: centralize the supported user-scope credential-name list and verify generated-launcher behavior for present, absent, newly added, and rotated keys without exposing values. |
| CXL-024 | P2 | Open | `a48bb85` | `scripts/install_windows_operational_tasks.ps1:26,191-195`; `assistant/paper_evidence.py:440-445`; contrast `scripts/install_windows_ml_shadow_tasks.ps1:98-108,148-160` | The paper-observation task defaults to 16:30 in the computer's local timezone, even though its description promises an after-NYSE-close capture. On a host in UTC or a timezone east of New York, the default runs before the NYSE close; the evidence layer correctly refuses it, so that session receives no immutable observation. The ML task installer already converts an Eastern market clock into local time and documents why. | Complete source-path proof: the operational default is `[datetime]::Today.AddHours(16).AddMinutes(30)` and is passed directly to `-At`; `capture_paper_observation` rejects any timestamp earlier than the exchange-calendar close. No setup wrapper supplies an override. | An otherwise successful default installation can silently miss every daily evidence capture solely because of host timezone, damaging a long-running epoch's completeness. Scheduled market tasks should be defined in exchange time, including DST, not wall-clock time. | Pending owner instruction. | Required: use the same Eastern-to-local conversion approach (plus half-day-safe timing) as the ML installer and test representative western, eastern, UTC, and DST-transition host zones. |

## 3. Claude whole-project-review disposition

Provisional disposition after independent reproduction:

- Accepted in implementation: FCS-001, FCS-002, FCS-003, FCS-005 through
  FCS-014, FCS-017, and FCS-018.
- Reopen: FCS-004, FCS-015, and the leap-day branch of FCS-016.
- Test-evidence debt remains for FCS-012 and FCS-013 even though their current
  implementations appear correct.

This section remains provisional until the exhaustive module pass is complete.

## 4. Validation record

- Baseline full suite: `3100 passed, 27 warnings` under Python 3.12.13 in
  427.24 seconds, with an explicit repository-local pytest base temp directory.
- The extra warnings relative to Claude's run were environment-only (physical
  core detection and pytest cache write permission); no test failed or skipped.
- Four PowerShell operational modules parse without syntax errors.

## 5. Module review progress

The exhaustive production, test, and PowerShell module dispositions will be
recorded here by directory as the line-by-line pass proceeds. A completed
directory means every line of every module in that directory was read; it does
not mean every module had a finding.

| Area | Modules | Status | Notes |
|---|---:|---|---|
| Root Python modules | 3 | Complete | No new confirmed finding; two low-risk validation candidates deferred pending caller/test cross-check. |
| `assistant/` | 65 | Complete | Includes execution kernel and LLM subpackages; CXL-001 through CXL-004 and CXL-006 through CXL-011. |
| `backtest/` | 6 | Complete | Comparator defect recorded as CXL-013. |
| `data/` | 9 | Complete | Optional-event failure path contributes to CXL-003. |
| `execution/` | 2 | Complete | No additional confirmed finding. |
| `ml/` | 37 | Complete | Immutable-evidence races, monitoring identity/counting defects, producer/consumer schema drift, and stale portfolio-target alignment recorded as CXL-014 through CXL-021. |
| `research/` | 2 | Complete | No additional confirmed finding. |
| `risk/` | 2 | Complete | No additional confirmed finding; remaining gate candidates require caller/test cross-check. |
| `scripts/` Python | 49 | Complete | Includes the full 4,272-line Streamlit UI; read-only command initialization recorded as CXL-022. |
| `signals/` | 19 | Complete | Non-finite shared scanner output recorded as CXL-012. |
| `strategies/` | 6 | Complete | Decline-grid comparator defect recorded as CXL-013. |
| `tests/` | 156 | Complete | Includes `tests/faults/`; cross-checked every source module and assertion inventory against the production paths. Important missing dangerous-direction cases are recorded in CXL-001, CXL-002, CXL-004, CXL-006 through CXL-011, and CXL-014 through CXL-024. |
| PowerShell operational scripts | 4 | Complete | Credential propagation and market-time scheduling defects recorded as CXL-023 and CXL-024. |

## 6. Review conclusion

The complete pass found no defect that can autonomously place a live order,
bypass human approval, or evade the kill switch, so there is no P0 or P1 at
this baseline. That is not a clean bill of health: the 19 P2 findings include
incorrect financial reporting, durable ledger/fill corruption, stale-event
state regression, policy lost updates, misleading or incomplete shadow
evidence, and races that can overwrite artifacts described as immutable.

No production, test, policy, database, scheduler, or epoch-runtime file was
changed during this review. The only changes are this finding record and the
cross-computer session handoff. `paper-epoch-002` remains outside this checkout
on its frozen stable instance and is not evidence for or against these fixes.
